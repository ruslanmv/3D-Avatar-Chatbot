# Scene background provenance

Ten scenes for the viewport background feature. Generated for this app's camera
(batch A19); the previous set is described below, because it is still in the
history and the difference between the two is the point.

## Where these files came from

Generated by
**[3D-Ambience-Studio](https://github.com/ruslanmv/3D-Ambience-Studio)** through
OpenAI's image API (`gpt-image-2.5-sunburst`, quality `high`), on 2026-09-13, by
`ambience generate-presets --live`.

Twenty plates: each scene is **two independent compositions**, not one picture
cropped twice.

|           | Landscape     | Portrait      |
| --------- | ------------- | ------------- |
| Requested | 1920×1088     | 1088×1920     |
| Published | **1920×1080** | **1080×1920** |

The request sizes are the API's, not ours: it requires both edges to be
multiples of 16, and 1080 is not one. The surplus rows are cropped, never
scaled, so the horizon stays on the row the camera contract put it on.

## Why they were regenerated

The previous ten were good photographs that happened to be behind her. These
were composed **for this app's camera**, which is the whole difference:

- horizon at **44.4%** of frame height in landscape and **46.1%** in portrait —
  not the middle, because `frameObject` tilts the eye about 1.72° down
- the character's feet at **89.7%** and **88.2%**, on continuous readable ground
- the band she occupies — **39.8–60.2%** of the width in landscape,
  **25.2–74.8%** in portrait — kept clear of prominent foreground

Every one of those figures came from `camera-contract.json` at generation time.
None is written into the art pipeline, so re-exporting the camera invalidates
art composed against the old one rather than leaving it looking finished.

Each package carries a `provenance.json` recording the model, the exact prompts,
their SHA-256, the calibration fingerprint and both sizes. No credential appears
in any of them — the key is read from the environment and never serialised.

## Licence

**Apache-2.0**, the same as this repository.

These are model outputs commissioned for this project from original prompts that
name no real or fictional place; the art direction lives in
`apps/api/ambience/scenes.py` in the Studio and the full prompts in each
package's `provenance.json`. Anything that later replaces these files must be
Apache-2.0 or compatible, with its real provenance recorded here.

## The set they replaced

Copied from **[`ruslanmv/yourfriend`](https://github.com/ruslanmv/yourfriend)**
at `public/ambient/{light,dark}/`, commit `60f421b` — photoreal, 1672×941, in
matched light/dark pairs, under that repository's
[Apache-2.0 `LICENSE`](https://github.com/ruslanmv/yourfriend/blob/master/LICENSE),
"Copyright 2024 Ruslan Magana". They are in this repository's history at
`4db2ada` and earlier.

One upstream inconsistency worth knowing about while it lasts: that repository's
`README.md` still carries a "Licensing" section saying no licence is declared
and to treat its imagery as all rights reserved. The `LICENSE` file is the
operative grant and post-dates that paragraph, but anyone auditing the chain
will read both.

## The fallback set

[`tools/ambience/generate-backgrounds.py`](../../tools/ambience/generate-backgrounds.py)
generates ten abstract stand-ins at the same ten paths — gradient skies, horizon
bands, silhouettes, water. They are **not** photoreal and are **not** what
ships.

They exist for the case where the photoreal set has to come out for any reason.
Their output is a deterministic function of a script in this repository, so they
are Apache-2.0 by construction, with no third-party provenance to trace at all —
not even a licence file in another repository. Run:

```bash
python3 tools/ambience/generate-backgrounds.py
```

and they overwrite the files below. Nothing else in the feature changes, because
the catalogue names **paths, not pixels**. That is the property that makes
either set droppable.

## The files

Every scene is two plates. The portrait one is a separate composition, not a
crop — the runtime picks between them by the shape of the canvas, which on this
layout is not the same thing as the shape of the device.

| Scene id                | Landscape                            | Portrait                                      | Reads as                                |
| ----------------------- | ------------------------------------ | --------------------------------------------- | --------------------------------------- |
| `ambient:ocean:day`     | `light/ocean-sunrise.webp`           | `light/ocean-sunrise-portrait.webp`           | Low sun over a calm sea, stone platform |
| `ambient:ocean:night`   | `dark/ocean-moonlight.webp`          | `dark/ocean-moonlight-portrait.webp`          | Moon and its reflection, open water     |
| `ambient:lake:day`      | `light/mountain-lake-day.webp`       | `light/mountain-lake-day-portrait.webp`       | Peaks mirrored in an alpine lake        |
| `ambient:lake:night`    | `dark/mountain-lake-night.webp`      | `dark/mountain-lake-night-portrait.webp`      | The same lake under a full moon         |
| `ambient:garden:day`    | `light/meditation-garden-day.webp`   | `light/meditation-garden-day-portrait.webp`   | Raked stone garden in soft daylight     |
| `ambient:garden:night`  | `dark/meditation-garden-night.webp`  | `dark/meditation-garden-night-portrait.webp`  | The same garden, lanterns at the edges  |
| `ambient:terrace:day`   | `light/coastal-terrace-day.webp`     | `light/coastal-terrace-day-portrait.webp`     | Pale terrace above a bright sea         |
| `ambient:terrace:night` | `dark/coastal-terrace-twilight.webp` | `dark/coastal-terrace-twilight-portrait.webp` | The same terrace at blue hour           |
| `ambient:sky:day`       | `light/open-sky-day.webp`            | `light/open-sky-day-portrait.webp`            | A high platform under wide cloud        |
| `ambient:sky:night`     | `dark/open-sky-starlight.webp`       | `dark/open-sky-starlight-portrait.webp`       | The same platform under stars           |

**Total: 5.9 MB across 20 files.**

## What the catalogue cannot satisfy

The resolver knows fourteen intents; this set answers the sea, lake, garden,
terrace and sky ones. There is **no forest, fire, rain, city or fantasy scene.**
That is a content gap, not a bug — `Resolver.satisfiableIntents()` computes the
companion's vocabulary from whatever is actually here, so she never offers to
take somebody to a forest and then quietly declines.
