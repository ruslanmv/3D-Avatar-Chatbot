'use strict';

/**
 * PoseHandleIK — Stub for Phase 3: IK drag handles.
 * Will add inverse kinematics for VR controller / touch-drag interaction.
 */
(function () {
    window.NEXUS_POSE_HANDLE_IK = {
        solveHandTarget: function () {
            console.info('[PoseHandleIK] Stub loaded. Add IK in phase 3.');
        },
    };
})();
